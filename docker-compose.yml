version: '3'

services:
    database:
        ports:
            - "27017:27017"

    backend:
        build: ./backend-build
        ports:
            - "9090:9090"
        depends_on:
            - database

    frontend:
        build: ./frontend-build
        ports:
            - "8080:8080"
        depends_on:
            - backend