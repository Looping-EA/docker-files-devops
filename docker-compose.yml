version: '3'

services:
    backend:
        build: ./backend-build
        ports:
            - "9090:9090"
        depends_on:
            - database
    
    database:
        ports:
            - "27017:27017"

    frontend:
        build: ./frontend-build
        ports:
            - "8080:8080"
        depends_on:
            - backend